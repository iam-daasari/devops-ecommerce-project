apiVersion: v1
kind: ConfigMap
metadata:
  name: shopease-config
  namespace: shopease
data:
  SPRING_PROFILES_ACTIVE: "production"
  SERVER_PORT: "8081"
  MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE: "health,info,metrics,prometheus"
  MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS: "always"
