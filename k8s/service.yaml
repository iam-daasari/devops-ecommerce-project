apiVersion: v1
kind: Service
metadata:
  name: shopease-service
  namespace: shopease
  labels:
    app: shopease
spec:
  type: LoadBalancer
  selector:
    app: shopease
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8081
